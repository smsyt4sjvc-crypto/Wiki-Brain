# ---- SECRETS: never commit these ----
.env
.env.*
!.env.example
secrets.*
config.local.*
*key*.json
*credentials*
alpaca.key

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/

# ---- journal / runtime (keep the format, not the live data) ----
journal/*.log
journal/*.jsonl
!journal/.gitkeep

# ---- OS / editor ----
.DS_Store
.vscode/
.idea/
