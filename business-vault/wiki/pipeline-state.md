# Pipeline State — the canonical current truth

> Only what is ACTUALLY true. Firewall: DATA = signed/quoted/owed. THESIS = expected/hoped, labeled.

## DATA (as of YYYY-MM-DD — fill on first real session)
- Active bids: (none logged yet)
- Jobs in progress: —
- AR / owed: —
- Committed material orders: —

## THESIS (interpretation — NOT fact)
- (reads on pipeline health, win-rate trends — labeled)
